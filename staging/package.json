{
  "name": "${project.artifactId}",
  "version": "${version.unrevise}",
  "description": "${project.description}",
  "scripts": {
    "postinstall": "gulp"
  },
  "dependencies": {
    "plotly.js-dist": "${version.unrevise}"
  },
  "devDependencies": {
    "gulp": "^4.0.0",
    "gulp-minify": "^3.1.0"
  }
}
